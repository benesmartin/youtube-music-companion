# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# Node (if a build step is added later)
node_modules/

# Packaged extension artifacts
*.zip
*.xpi
*.crx
web-ext-artifacts/

# Private dev notes (backed up manually)
NOTES.md
ROADMAP.md

# Store/listing assets (hosted via GitHub release attachments)
promo/

# Landing page (published separately, not part of the extension source)
docs/

# Personal userscripts (not part of the extension)
userscripts/

# Private test suite (runs against unmodified production files)
tests/

# Private e2e harness (Playwright against real YTM; needs a logged-in profile)
e2e/
package.json
package-lock.json
playwright.config.js
test-results/
